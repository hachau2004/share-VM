✅ GIẢI PHÁP CHẮC CHẮN SỬA ĐƯỢC

Chạy 3 lệnh này — để ép cấu hình đúng chỗ:

cd ~/fabric-samples/bankchain
export FABRIC_CFG_PATH=$PWD/configtx
echo $FABRIC_CFG_PATH


Kết quả phải in ra:

/home/hachau/fabric-samples/bankchain/configtx


Sau đó chạy lại:

configtxgen -profile BankchainChannel \
  -outputCreateChannelTx ./channel-artifacts/channel.tx \
  -channelID bankchannel

✅ Kiểm tra cuối cùng: đúng file configtx chưa?
head -n 20 $FABRIC_CFG_PATH/configtx.yaml


Phải thấy:

Profiles:
  BankchainGenesis:
  BankchainChannel:

🔥 Nếu anh gửi em output của:

✅ echo $FABRIC_CFG_PATH
✅ ls -l $FABRIC_CFG_PATH
✅ grep -n "BankchainChannel" $FABRIC_CFG_PATH/configtx.yaml
