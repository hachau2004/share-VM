✅ GIẢI PHÁP CHÍNH XÁC — KIỂM TRA & SỬA LẠI
✅ 1. MỞ FILE CHÍNH XÁC:

Chạy:

nano ~/fabric-samples/bankchain/docker-compose-ca.yaml

✅ 2. TÌM TẤT CẢ CÁC DÒNG CHỨA:
hyperledger/fabric-ca


Bạn có thể search trong nano:
Nhấn Ctrl + W, gõ:

fabric-ca

✅ 3. ĐẢM BẢO 2 DÒNG NÀY ĐÚNG 100%:

Thay:

❌ Sai:

image: hyperledger/fabric-ca:${IMAGE_TAG}


✅ Đúng:

image: hyperledger/fabric-ca:1.5.11


⚠️ DỰ ÁN NÀY THƯỜNG CÓ 2 SERVICE CA:

ca0

ca1

👉 Bạn phải sửa cả hai, nếu không Docker Compose vẫn tìm 3.1.

✅ 4. LƯU FILE

Trong nano:

Ctrl + O, Enter

Ctrl + X

✅ 5. XOÁ CONTAINER & IMAGE CŨ ĐỂ TRÁNH NÓ GIỮ TAG 3.1**
docker rm -f $(docker ps -aq)
docker rmi hyperledger/fabric-ca:3.1 || true

✅ 6. CHẠY LẠI CA
cd ~/fabric-samples/bankchain
docker compose -f docker-compose-ca.yaml up -d

✅ 7. KIỂM TRA KẾT QUẢ
docker ps
