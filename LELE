export CHANNEL_NAME=bankchannel

configtxgen -profile BankchainChannel \
  -outputCreateChannelTx ./channel-artifacts/channel.tx \
  -channelID $CHANNEL_NAME
Bạn cần chạy trong thư mục:

bash
Sao chép mã
fabric-samples/bankchain
✅ 3️⃣ Sau khi tạo xong channel.tx → vào lại CLI để tạo channel
bash
Sao chép mã
docker exec -it cli bash
export CHANNEL_NAME=bankchannel
Sau đó:

bash
Sao chép mã
peer channel create \
  -o orderer.example.com:7050 \
  -c $CHANNEL_NAME \
  -f ./channel-artifacts/channel.tx \
  --outputBlock ./channel-artifacts/bankchannel.block \
  --tls \
  --cafile /opt/gopath/src/github.com/hyperledger/fabric/peer/crypto/ordererOrganizations/example.com/orderers/orderer.example.com/tls/ca.crt
✅ Bạn đang làm đúng hướng rồi
Nếu cần mình check lại configtx.yaml hoặc docker-compose để đảm bảo không lỗi nữa — bạn chỉ cần gửi lên.












