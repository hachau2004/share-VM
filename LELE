✅ ✅ GIẢI PHÁP (CHÍNH XÁC 100%)

Bạn cần sửa file:

docker-compose-ca.yaml


đổi dòng:

image: hyperledger/fabric-ca:${IMAGE_TAG}


→ thành:

image: hyperledger/fabric-ca:1.5.11


Vì Fabric-CA phải chạy phiên bản 1.5.x (ARM64 hỗ trợ đầy đủ).

✅ BƯỚC 1 — SỬA docker-compose-ca.yaml

Chạy:

nano ~/fabric-samples/bankchain/docker-compose-ca.yaml


Tìm 2 chỗ như:

image: hyperledger/fabric-ca:${IMAGE_TAG}


Sửa thành:

image: hyperledger/fabric-ca:1.5.11


👉 Lưu lại:
Nhấn CTRL + O, Enter rồi CTRL + X.

✅ BƯỚC 2 — XOÁ CONTAINER CŨ (NẾU CÓ)
docker rm -f $(docker ps -aq)

✅ BƯỚC 3 — KHOI ĐỘNG LẠI CA
cd ~/fabric-samples/bankchain
docker compose -f docker-compose-ca.yaml up -d

✅ BƯỚC 4 — KIỂM TRA
docker ps


Bạn sẽ thấy:

ca.banka.example.com

ca.bankb.example.com

✅ CA chạy thành công
✅ Không còn “manifest unknown”
✅ Không còn lỗi IMAGE_TAG

✅ TIẾP THEO CHÚNG TA SẼ START ORDERER

KHI NÀO CA chạy OK → mình sẽ dẫn bạn start Orderer + CLI → tạo channel → join peer.

👉 HÃY GỬI MÌNH ẢNH KẾT QUẢ CỦA:

docker ps
