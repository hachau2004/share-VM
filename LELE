git clone https://github.com/hyperledger/fabric-samples.git --depth 1
sudo docker pull hyperledger/fabric-peer:3.1
sudo docker pull hyperledger/fabric-orderer:3.1
sudo docker pull hyperledger/fabric-tools:3.1
sudo docker pull hyperledger/fabric-ccenv:3.1
sudo docker pull hyperledger/fabric-ca:1.5.10

sudo docker images | grep hyperledger

sudo docker pull ghcr.io/hyperledger/fabric-peer:3.1
sudo docker pull ghcr.io/hyperledger/fabric-orderer:3.1
sudo docker pull ghcr.io/hyperledger/fabric-tools:3.1
sudo docker pull ghcr.io/hyperledger/fabric-ccenv:3.1
sudo docker pull ghcr.io/hyperledger/fabric-ca:1.5.10
(Chú ý: fabric-ca vẫn là 1.5.10, chưa có bản 3.x.)

🔍 Sau khi chạy xong, kiểm tra lại:
bash
Sao chép mã
sudo docker images | grep hyperledger

🧩 Bước 1: Kiểm tra kết nối mạng trong máy ảo
Chạy:
ping -c 3 google.com

Nếu không thấy phản hồi (ping: unknown host google.com hoặc không có reply), thì máy ảo chưa có Internet.
✅ Cách khắc phục:


Mở VirtualBox → chọn VM Ubuntu → Settings → Network


Ở “Attached to” chọn NAT


Khởi động lại Ubuntu rồi thử lại ping google.com.



🧩 Bước 2: Nếu vẫn lỗi timeout
Có thể Docker bị giới hạn DNS.
Chạy:
sudo systemctl restart docker

Rồi thử lại:
sudo docker pull ghcr.io/hyperledger
