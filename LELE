BƯỚC 1 — EXPORT 3 BIẾN MÔI TRƯỜNG BẮT BUỘC

Chạy 3 lệnh sau trong terminal Ubuntu:

export BANKCHAIN_CA1_PRIVATE_KEY=priv_sk
export BANKCHAIN_CA2_PRIVATE_KEY=priv_sk
export IMAGE_TAG=3.1

✅ BƯỚC 2 — KIỂM TRA XEM ĐÃ EXPORT ĐÚNG CHƯA
echo $BANKCHAIN_CA1_PRIVATE_KEY
echo $BANKCHAIN_CA2_PRIVATE_KEY
echo $IMAGE_TAG


Kết quả phải là:

priv_sk
priv_sk
3.1

✅ BƯỚC 3 — KHỞI ĐỘNG CA (KHÔNG BÁO LỖI NỮA)
cd ~/fabric-samples/bankchain
docker compose -f docker-compose-ca.yaml up -d


✅ Lần này CA sẽ start OK
✅ Không còn cảnh báo private_key
✅ Không còn cảnh báo image_tag

✅ BƯỚC 4 — KIỂM TRA CA ĐANG CHẠY

Chạy:

docker ps
