BƯỚC 1 — LẤY PRIVATE KEY CỦA BANK A CA

Chạy:

cd ~/fabric-samples/bankchain/crypto-config/peerOrganizations/banka.example.com/ca
ls -l


Bạn sẽ thấy:

ca.banka.example.com-cert.pem

1 file tên dài kết thúc _sk → Đây là PRIVATE KEY

👉 Gửi mình ảnh output để xác nhận tên file _sk

✅ BƯỚC 2 — LẤY PRIVATE KEY CỦA BANK B CA

Chạy:

cd ~/fabric-samples/bankchain/crypto-config/peerOrganizations/bankb.example.com/ca
ls -l


Bạn sẽ thấy:

ca.bankb.example.com-cert.pem

1 file _sk

👉 Gửi mình ảnh output này luôn

✅ BƯỚC 3 — MÌNH SẼ TẠO CHO BẠN 2 LỆNH EXPORT:

Ví dụ:

export BANKCHAIN_CA1_PRIVATE_KEY=xxxx_sk
export BANKCHAIN_CA2_PRIVATE_KEY=yyyy_sk


và thêm:

export IMAGE_TAG=3.1

✅ BƯỚC 4 — START CA SAU KHI ĐÃ EXPORT

Chạy:

cd ~/fabric-samples/bankchain
docker compose -f docker-compose-ca.yaml up -d
