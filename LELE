gDi chuyển vào thư mục fabric-samples
(đây là thư mục bạn đã git clone trước đó):

cd ~/fabric-samples


Chạy script để tải Fabric binaries và config files
(script này tự động tải peer, orderer, fabric-ca-client...):

curl -sSL https://bit.ly/2ysbOFE | bash -s -- 3.1.0 1.5.10


💡 Giải thích:

3.1.0 là version của Fabric binaries

1.5.10 là version của Fabric CA

Quá trình này sẽ mất vài phút.

Sau khi tải xong, thêm binary vào PATH:

export PATH=${PWD}/bin:$PATH
echo 'export PATH=${PWD}/bin:$PATH' >> ~/.bashrc
source ~/.bashrc


Kiểm tra lại:

peer version
orderer version
fabric-ca-client version


👉 Nếu hiển thị version như:

peer:
 Version: 3.1.0
fabric-ca-client:
 Version: 1.5.10
