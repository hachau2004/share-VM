✅ FILE CONFIGTX.YAML CHUẨN
---
Organizations:

    - &OrdererOrg
        Name: OrdererOrg
        ID: OrdererMSP
        MSPDir: ../crypto-config/ordererOrganizations/example.com/msp

    - &BankA
        Name: bankaMSP
        ID: bankaMSP
        MSPDir: ../crypto-config/peerOrganizations/banka.example.com/msp
        AnchorPeers:
            - Host: peer0.banka.example.com
              Port: 7051

    - &BankB
        Name: bankbMSP
        ID: bankbMSP
        MSPDir: ../crypto-config/peerOrganizations/bankb.example.com/msp
        AnchorPeers:
            - Host: peer0.bankb.example.com
              Port: 9051

Capabilities:
    Channel: &ChannelCapabilities
        V2_0: true
    Orderer: &OrdererCapabilities
        V2_0: true
    Application: &ApplicationCapabilities
        V2_0: true

Application: &ApplicationDefaults
    Organizations:
    Capabilities:
        <<: *ApplicationCapabilities

Orderer: &OrdererDefaults
    OrdererType: etcdraft
    Addresses:
        - orderer.example.com:7050
    EtcdRaft:
        Consenters:
            - Host: orderer.example.com
              Port: 7050
              ClientTLSCert: ../crypto-config/ordererOrganizations/example.com/orderers/orderer.example.com/tls/server.crt
              ServerTLSCert: ../crypto-config/ordererOrganizations/example.com/orderers/orderer.example.com/tls/server.crt
    Organizations:
    Capabilities:
        <<: *OrdererCapabilities

Profiles:

    BankchainGenesis:
        Orderer:
            <<: *OrdererDefaults
            Organizations:
                - *OrdererOrg
        Consortium: SampleConsortium
        Capabilities:
            <<: *ChannelCapabilities

    BankchainChannel:
        Consortium: SampleConsortium
        Application:
            <<: *ApplicationDefaults
            Organizations:
                - *BankA
                - *BankB
            Capabilities:
                <<: *ApplicationCapabilities

Consortiums:
    SampleConsortium:
        Organizations:
            - *BankA
            - *BankB

✅ SAU KHI THAY FILE, CHẠY LẠI:
1️⃣ Xuất biến môi trường
export FABRIC_CFG_PATH=$PWD/configtx
export CHANNEL_NAME=bankchannel

2️⃣ Xóa file cũ (nếu có)
rm -rf channel-artifacts/*.*

3️⃣ Tạo channel.tx
configtxgen -profile BankchainChannel \
  -outputCreateChannelTx ./channel-artifacts/channel.tx \
  -channelID $CHANNEL_NAME


✅ Nếu file đúng → sẽ ra KHÔNG lỗi và tạo được channel.tx
