sudo mkdir -p /usr/local/lib/docker/cli-plugins
sudo curl -fSL "https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64" -o /usr/local/lib/docker/cli-plugins/docker-compose
sudo chmod +x /usr/local/lib/docker/cli-plugins/docker-compose

sudo apt update
bash
Sao chép mã
sudo apt install -y ca-certificates curl gnupg
Tạo thư mục lưu khóa bảo mật Docker:

bash
Sao chép mã
sudo install -m 0755 -d /etc/apt/keyrings
Thêm khóa GPG của Docker (để Ubuntu tin tưởng repo Docker):

bash
Sao chép mã
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
Thêm repo Docker chính thức:

bash
Sao chép mã
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
Cập nhật lại danh sách package:

bash
Sao chép mã
sudo apt update
Cài Docker:

bash
Sao chép mã
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
⚙️ Bước 2: Kiểm tra Docker đã cài xong chưa
bash
Sao chép mã
docker --version
hoặc:

bash
Sao chép mã
sudo docker run hello-world
→ Nếu thấy dòng:

Hello from Docker!

là OK ✅

⚙️ Bước 3 (tùy chọn): Chạy Docker không cần sudo
bash
Sao chép mã
sudo usermod -aG docker $USER
Rồi tắt và bật lại máy ảo Ubuntu.

📌 Lưu ý đặc biệt (vì bạn dùng Mac):
Nếu Mac của bạn là chip M1/M2/M3 (Apple Silicon) → bạn đang dùng Ubuntu ARM.
Thì ở bước tải Docker Compose bạn cần đổi URL thành:

bash
Sao chép mã
sudo curl -fSL "https://github.com/docker/compose/releases/latest/download/docker-compose-linux-aarch64" \
  -o /usr/local/lib/docker/cli-plugins/docker-compose

TỪ ĐÂY THÌ TIẾP

docker --version
sudo usermod -aG docker $USER

curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt-get install nodejs -y

nodejs –v 
npm -v

wget https://go.dev/dl/go1.24.0.linux-amd64.tar.gz
sudo tar -C /usr/local -xzf go1.24.0.linux-amd64.tar.gz

export GOROOT=/usr/local/go
export GOPATH=$HOME/go
export PATH=$PATH:$GOROOT/bin:$GOPATH/bin

source ~/.profile
go version
git clone https://github.com/hyperledger/fabric-samples.git
cd fabric-samples

curl -sSL https://bit.ly/2ysbOFE | bash -s -- 3.1.0 1.5.10
docker images | grep hyperledger

docker pull hyperledger/fabric-peer:3.1
docker pull hyperledger/fabric-orderer:3.1
docker pull hyperledger/fabric-tools:3.1
docker pull hyperledger/fabric-ccenv:3.1
docker pull hyperledger/fabric-ca:1.5.10

docker images | grep hyperledger
