✅ Cách kiểm tra dung lượng thật sự

Chạy:

df -h


Bạn sẽ thấy / hoặc /home = 100% full.

✅ 3 cách dọn dẹp nhanh nhất để chạy lại được
✅ 1. Xóa toàn bộ image + container Docker cũ (giải phóng vài GB)

Chạy 3 lệnh:

docker system prune -a --volumes -f
docker volume prune -f
docker network prune -f


Thường giải phóng 3–10GB.

✅ 2. Xóa cache apt (giải phóng 1–2GB)
sudo apt-get clean
sudo apt-get autoremove -y

✅ 3. Xóa file trong thư mục Docker bị phình to
sudo rm -rf /var/lib/docker/overlay2/*


⚠️ Cách này xóa toàn bộ data Docker, nhưng hoàn toàn an toàn nếu bạn đang reset môi trường.

✅ Sau khi dọn xong → kiểm tra lại
df -h


Bạn cần ít nhất:

✅ 10GB trống để chạy Hyperledger Fabric
✅ Nếu dưới 6GB → vẫn lỗi tiếp

✅ Khi disk đã thoáng → bạn quay lại chạy compose
export DOCKER_DEFAULT_PLATFORM=linux/arm64
docker compose -f docker-compose-bankchain.yaml up -d

👉 Gửi lại mình output của:
df -h


Tôi sẽ xem dung lượng và chỉ cách tối ưu nếu còn thiếu.
