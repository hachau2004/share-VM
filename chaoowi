) Kiểm tra CHI TIẾT GIAO DỊCH bằng transaction hash

Bạn đã có TX hash:

fe44b0a11e24e6d1b75c87c26fcd3dda3700c1f92f902b142d95f5ccafbded1b

Chạy:

curl -s "http://localhost:8000/transactions/fe44b0a11e24e6d1b75c87c26fcd3dda3700c1f92f902b142d95f5ccafbded1b/operations" | jq .


👉 Kết quả đúng sẽ có:

"type": "payment"

"asset_code": "USDX"

"from": "<BANKA_PUBLIC>"

"to": "<BANKB_PUBLIC>"

"amount": "500" (hoặc đúng số bạn chuyển)

✅ 2) Kiểm tra BALANCE của BankA và BankB
BankA:
curl -s "http://localhost:8000/accounts/${BANKA_PUBLIC}" | jq '.balances'


Kết quả đúng:

USDX giảm (10000 - 500 = 9500 nếu bạn chuyển 500)

XLM không đổi nhiều (trừ phí giao dịch nhỏ)

BankB:
curl -s "http://localhost:8000/accounts/${BANKB_PUBLIC}" | jq '.balances'


Kết quả đúng:

Xuất hiện USDX đầu tiên
ví dụ:

{
  "asset_type": "credit_alphanum4",
  "asset_code": "USDX",
  "asset_issuer": "<ISSUER_PUBLIC>",
  "balance": "500.0000000"
}

✅ 3) Kiểm tra lịch sử payments của BankB
curl -s "http://localhost:8000/accounts/${BANKB_PUBLIC}/payments?order=desc&limit=10" | jq '.'


Kết quả đúng sẽ thấy 1 entry:

"type": "payment",
"from": "<BANKA_PUBLIC>",
"to": "<BANKB_PUBLIC>",
"asset_code": "USDX",
"amount": "500"

🎉 Khi bạn chạy xong 3 lệnh trên, hãy gửi mình 3 screenshot kết quả.
