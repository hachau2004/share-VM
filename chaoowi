✅ BƯỚC TIẾP THEO — KIỂM TRA TRẠNG THÁI PEER0.BANKA

Chạy:

docker ps | grep peer0.banka


Và:

docker logs peer0.banka.example.com --tail=50


👉 Gửi mình log này!

✅ BƯỚC 2 — TEST DNS TỪ CHÍNH CONTAINER PEER (không phải CLI)

Kiểm tra DNS từ peer:

docker exec -it peer0.banka.example.com sh -c "getent hosts peer0.banka.example.com"


Nếu trong peer container vẫn không resolve được → lỗi Docker DNS.

✅ BƯỚC 3 — TEST DNS TỪ CLI THEO CÁCH KHÁC

Trong CLI thử:

nslookup peer0.banka.example.com


Nếu báo command not found, thử:

apt update && apt install -y dnsutils
nslookup peer0.banka.example.com

✅ GIẢI THÍCH LÝ DO getent hosts TRẢ VỀ RỖNG

getent hosts trả rỗng khi:

Peer container chưa đăng ký tên trong Docker DNS

Peer container khởi động lỗi

Docker network chưa đồng bộ DNS (lúc mới chạy)

Docker compose override extra_hosts (không có trong file của bạn)

✅ BƯỚC CUỐI — XÁC NHẬN LẠI HOSTNAME TRONG docker-compose

Bạn chạy:

docker inspect peer0.banka.example.com | grep Hostname
docker inspect peer0.banka.example.com | grep -A3 Networks


Gửi mình kết quả, để kiểm tra:

✅ Hostname đúng chưa
✅ Container có đúng network chưa
✅ DNS alias có đúng không
